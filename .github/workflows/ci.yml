name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    name: "Test"
    timeout-minutes: 90
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ ubuntu-latest ]

    steps:
      - uses: actions/checkout@v2

      - name: "Set up environment"
        run: |
          set -euxo pipefail
          chmod -R 700 .

      - name: "Test system software installation"
        run: sudo install/system_software.sh

      - name: "Test python applications and libraries installation"
        run: sudo install/python_software.sh

      - name: "Test configuration set up"
        run: sudo install/configuration.sh

      - name: "Test autostart applications set up"
        run: sudo install/autostart_apps.sh

      - name: "Test default applications set up"
        run: sudo install/default_apps.sh

  lint:
    name: "Lint"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - run: make setup

      - run: make lint
